<resources>
    <!-- Main Themes -->
    <style name="MainTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashTheme" parent="MainTheme">
        <item name="android:windowBackground">@drawable/ic_splash_screen</item>
    </style>

    <!-- Toolbar Themes -->

    <style name="ToolBarTheme">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <!-- Text Themes -->
    <style name="smallTextTheme" parent="TextAppearance.AppCompat.Small">
        <item name="android:fontFamily">@font/times</item>
    </style>

    <style name="Display2TextTheme" parent="TextAppearance.AppCompat.Display1">
        <item name="android:fontFamily">@font/times</item>
    </style>
</resources>
